import { PrimerPage } from './app.po';

describe('Open Access', function() {
  let page: PrimerPage;

  beforeEach(() => {
    page = new PrimerPage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
